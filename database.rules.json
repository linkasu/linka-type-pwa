{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "factory": {
      ".read": "auth.uid!=null",
      ".write": false

    }
  }
}